import { Inter, Montserrat } from 'next/font/google';

export const defaultFont = Inter({ subsets: ['latin'] });
export const TITLE_FONT = Montserrat({ subsets: ['latin'], weight: ['500', '600', '700'] });
